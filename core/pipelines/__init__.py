"""
core.pipelines — Pipeline execution engine.

Contains the PipelineContext data object, PipelineRunner for sequential
execution, PipelineBuilder for config-driven pipeline composition,
and all pipeline components.
"""
