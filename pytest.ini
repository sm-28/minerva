[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    integration: marks tests that require real external services (DB, S3, AWS)
    unit: marks pure unit tests with no external dependencies
