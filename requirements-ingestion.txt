# Ingestion service dependencies
# Install with: pip install -r requirements-ingestion.txt

# ── Core ingestion pipeline ───────────────────────────────────────────────────
pdfplumber>=0.10.0          # PDF text extraction (preferred)
PyPDF2>=3.0.0               # PDF fallback
python-docx>=1.1.0          # DOCX support
sentence-transformers>=2.7.0  # Sentence embedding models
faiss-cpu>=1.8.0            # Vector index (use faiss-gpu for GPU instances)
numpy>=1.26.0

# ── AWS ───────────────────────────────────────────────────────────────────────
boto3>=1.34.0

# ── Database ──────────────────────────────────────────────────────────────────
asyncpg>=0.29.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
reportlab>=4.0.0            # Generates PDF fixtures for parser tests
