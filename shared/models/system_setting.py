"""
shared/models/system_setting.py — Global system settings model.

Table: public.system_settings

Fields:
    key (PK), value (JSONB), description

Used for global defaults like default provider selections,
cost estimation formulas, and feature flags.
"""
