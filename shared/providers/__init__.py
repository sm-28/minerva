"""
shared.providers — Provider abstraction layer.

Contains base interfaces for STT, LLM, TTS, and Translation providers,
concrete implementations for each vendor, and the ProviderResolver
singleton for dynamic provider selection.
"""
